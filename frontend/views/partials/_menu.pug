
nav.navbar.navbar-inverse
    .container
        .navbar-header
            button.navbar-toggle.collapsed(
                    type="button"
                    data-toggle="collapse"
                    data-target="#bs-menu"
                    aria-expanded="false"
                )
                span.sr-only Menu
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href="/").
                Google Captha + two factor
        .collapse.navbar-collapse#bs-menu
            ul.nav.navbar-nav
                each item in menu
                    li
                        a(href=item.link)= item.name
